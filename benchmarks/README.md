# Benchmarks folder

Each iteration should generate:

- `<tag>.json` with numerical results
- `<tag>_paths.png` showing model trajectories
- `<tag>_progress.png` showing checkpoints/completion
- `<tag>_overlay.png` comparing expert data and model behavior

Do not delete benchmark outputs. They prove the iteration process.
